/**
 * App-level utility functions.
 *
 * This directory is for pure, reusable helpers that are not tied to any
 * specific feature (e.g. formatters, parsers, date helpers, validators).
 *
 * The shadcn `cn()` helper lives in lib/utils.ts by convention and
 * should not be moved — the shadcn CLI writes to that path.
 */

export { cn } from "@/lib/utils"
